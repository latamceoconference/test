# Exemplo para ambiente local.
# 1) Copie este arquivo para `lensstore/.env.local`
# 2) Preencha os valores e rode `npm run dev`

# URL do app (usado para gerar back_urls do Mercado Pago)
NEXT_PUBLIC_SITE_URL=http://localhost:4000

# Mercado Pago (se vazio, retorna URL de mock para testar o fluxo)
MERCADOPAGO_ACCESS_TOKEN=

# Supabase (catálogo, pedidos e imagens)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# (Servidor) usado para rotinas administrativas.
# Nunca exponha no navegador (mantenha apenas no .env.local / Vercel env).
SUPABASE_SERVICE_ROLE_KEY=

# WhatsApp (mostra botão flutuante no canto direito inferior)
# Exemplo: +55 (11) 99999-9999  -> 5511999999999
NEXT_PUBLIC_WHATSAPP_PHONE=



